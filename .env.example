# Variáveis de ambiente — Monitor de Ruptura Lojas Giovanna
# Copie para .env se necessário:
# copy .env.example .env (Windows)
# cp .env.example .env (Linux/Mac)

# Nenhuma variável de ambiente é obrigatória no modo --local.
# O pipeline lê dados de data/raw_data.json por padrão.
# Este arquivo existe apenas como referência para configuração futura.
