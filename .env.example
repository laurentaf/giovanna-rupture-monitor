# Variaveis de ambiente — Monitor de Ruptura Lojas Giovanna
# Copie para .env e configure o token:
#   copy .env.example .env   (Windows)
#   cp .env.example .env      (Linux/Mac)

# Token da API DataMission (obrigatorio)
# Gere na aba Projetos Ativos (botao GERAR API KEY)
API_TOKEN=seu-token-aqui
